<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" href="/" style="color:red; font-weight: bold;">EasyBuy</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li>
          <%= link_to(t("navbar.Products"), products_path) %>
        </li>
        <li>
          <%= link_to(t("navbar.About"), "/pages/about") %>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li>
          <%= link_to carts_path do  %>
            <%= t("navbar.Cart") %> <i class="fa fa-shopping-cart"> </i> ( <span id="product-cart-<%= @product_id %>"> <%= current_cart.items.size %> </span> )
          <% end %>
        </li>

        <% if !current_user %>
          <li> <%= link_to("登入", new_user_session_path) %> </li>
          <li> <%= link_to "Facebook 登入", user_omniauth_authorize_path(:facebook) %> </li>
        <% else %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Hello! , <%= current_user.name %>
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">

              <% if current_user.admin? %>
                <li>
                  <%= link_to("Admin 選單", admin_products_path ) %>
                </li>
              <% end %>
              <li>
                <%= link_to("個人訂單查詢", account_orders_path ) %>
              </li>
              <li role="separator" class="divider"></li>
              <li class="dropdown-header">
                語系
              </li>
              <li>
                <%= link_to "中文版", :controller => controller_name, :action => action_name, :locale => "zh-TW" %>
              </li>
              <li>
                <%= link_to "English", :controller => controller_name, :action => action_name, :locale => "en" %>
              </li>
              <li role="separator" class="divider"></li>
              <li> <%= link_to("帳戶設定", edit_user_registration_path )%>
              </li>
              <li>
                <%= link_to("登出", destroy_user_session_path, method: :delete ) %>
              </li>
            </ul>
          </li>
        <% end %>

      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
